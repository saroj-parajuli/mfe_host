const currentYear: number = new Date().getFullYear();
const CONTENTS = Object.freeze({
  COPYRIGHT: `Copyright © ${currentYear}. All rights reserved.`,
  BACK_TO_HOME: 'Back to Home',
});

export const FOOTER_CONTENTS = Object.freeze({
  CONTENTS,
});

